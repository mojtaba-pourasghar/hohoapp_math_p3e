# هوهو ریاضی — یادداشت‌های پروژه

اپ اندروید (جاوا) آموزش ریاضی سوم دبستان، بر اساس کتاب درسی ۱۴۰۴.

## قاعده‌ی همیشگی: بعد از هر تغییر

۱. **کامپایل کن.** Android SDK در محیط توسعه در دسترس نیست (میزبان `dl.google.com`
   با سیاست سازمانی بسته است)، پس بیلد روی GitHub Actions انجام می‌شود:
   `.github/workflows/build-apk.yml`. بعد از پوش، وضعیت ران را از
   `api.github.com` چک کن تا مطمئن شوی سبز شده.

۲. **ورژن بزن.** قبل از پوش، فایل `VERSION` را بالا ببر:
   - اصلاح باگ یا تغییر کوچک → رقم سوم (`1.1.0` → `1.1.1`)
   - قابلیت جدید → رقم دوم (`1.1.1` → `1.2.0`)

   `versionCode` خودکار از همین فایل ساخته می‌شود؛ دست نزن.

۳. **خروجی همیشه در همین آدرس منتشر می‌شود** — این آدرس ثابت است و عوض نمی‌شود:

   <https://github.com/mojtaba-pourasghar/hohoapp_math_p3e/releases/tag/latest>

   لینک مستقیم دانلود (نامش هیچ‌وقت تغییر نمی‌کند):
   `https://github.com/mojtaba-pourasghar/hohoapp_math_p3e/releases/download/latest/hoohoo-math.apk`

   بیلد خودش این‌ها را انجام می‌دهد:
   - ریلیز `latest` را با APK تازه بازسازی می‌کند (همان آدرس بالا)
   - ریلیز `v<VERSION>` را هم می‌سازد تا نسخه‌های قبلی در دسترس بمانند
   - `dist/hoohoo-math.apk` را در مخزن کامیت می‌کند

۴. **APK را برای کاربر بفرست.** از ریلیز `latest` دانلودش کن و با فایل تحویل بده.

> پوش‌هایی که خودِ اکشن انجام می‌دهد، ورک‌فلو را تریگر نمی‌کنند. اگر کامیتی را
> ربات پوش کرده (مثل فایل‌های صوتی)، بیلد را دستی dispatch کن.

> اگر مرحله‌ی انتشار شکست، APK همان بیلد در `dist/hoohoo-math.apk` کامیت شده و از همان‌جا
> قابل تحویل است. خودِ ورک‌فلو هم در صورت شکست یک ایشو با دلیل خطا باز می‌کند.

## صداها

- **گفتارهای هوهو**: هر جمله یک کلید دارد و فایلش در `res/raw/<key>.ogg` است.
  فهرست کامل با متن دقیق: `res/raw/audio_manifest.txt` — این فایل **با دست نوشته نمی‌شود**؛
  از خودِ درس‌ها ساخته می‌شود:

  ```
  python3 tools/make_manifest.py
  ```

  سرویس تبدیل متن به گفتار از محیط توسعه بسته است، پس فایل‌های صوتی روی رانر ساخته
  می‌شوند (صدای `fa-IR-DilaraNeural`). **بیلد APK خودش این کار را می‌کند** و فایل‌های
  تازه را کنار APK کامیت می‌کند؛ `make_voice.py` هر فایلی که هست را رد می‌کند، پس
  وقتی متن تازه‌ای اضافه نشده باشد این مرحله چند ثانیه است.
  ورک‌فلو `.github/workflows/voice.yml` هم برای ساخت دستی (مثلاً با `--force`) باقی است.
  کلیدهایی که به `x` ختم می‌شوند («یک مثال دیگر») با سرعت کمتری ضبط می‌شوند
  (`--example-rate`, پیش‌فرض ‎-۲۵٪) چون کودک در مثال به وقت بیشتری نیاز دارد.
- **خواندن سؤال‌های تمرین/کاربرگ/آزمون**: این سؤال‌ها خودکار ساخته می‌شوند، پس نمی‌شود
  برای هرکدام یک فایل ضبط کرد. به‌جایش **واژه‌ها** ضبط شده‌اند: عددهای ۰ تا ۱۰۰، صدگان‌ها،
  «هزار» و «و» (`n_*`, `va`)، و هر تکه‌ی ثابتِ متنِ قالب‌های `QuestionGenerator`
  (`q_<هش>`). `tts/QuestionVoice` سؤال را سرِ عددها تکه می‌کند و
  `LessonAudio.playSequence` کلیپ‌ها را پشت هم پخش می‌کند. اگر حتی یک کلیپ نباشد،
  کلِ جمله با موتور گفتار دستگاه خوانده می‌شود تا نصفه‌نیمه نشود.
  نامِ کلیپِ متن = `q_` + ده رقمِ اولِ SHA-1 (هگزِ دستی، چون locale فارسی است) —
  همین را `tools/make_manifest.py` هم حساب می‌کند؛ اگر یکی را عوض کردی، آن یکی را هم عوض کن.
- **صداهای کوتاه** (`sfx_*`, `bgm_calm`): با موج‌سازی ساخته شده‌اند، اسکریپتش در
  تاریخچه‌ی گیت است. لازم نیست دوباره ساخته شوند.
- اگر فایل صوتی نباشد، اپ همان متن را با موتور گفتار دستگاه می‌خواند.

## نکته‌های فنی که قبلاً گاز گرفته‌اند

- `<circle>` در vector drawable اندروید وجود ندارد؛ باید `<path>` با کمان نوشت.
- انیمیشن کاراکتر با `Choreographer` اجرا می‌شود نه `ValueAnimator` — انیماتور از
  «مقیاس مدت انیمیشن» سیستم تبعیت می‌کند و روی دستگاهی که انیمیشن خاموش است
  هوهو یخ می‌زند.
- `ContextCompat.getColor(c, 0)` استثنا پرتاب می‌کند؛ شناسه‌ی رنگ صفر نده.
- **هر کنترلی که به `layout/` اضافه می‌کنی، باید در `layout-land/` و `layout-sw600dp/` هم باشد.**
  یک بار «سؤال قبل/بعد» فقط به لی‌اوت گوشی اضافه شد و روی تبلت `findViewById` نال برگرداند و
  اپ کرش کرد. حالا `QuizFragment` و `LessonFragment` با `onTap`/`setText` نال‌امن بسته می‌شوند،
  ولی باز هم لی‌اوت‌ها را هم‌تراز نگه دار.
- مولد سؤال‌ها قبلاً فرمول خطی داشت و برای بعضی (k, span)ها جمله‌ی i حذف می‌شد و **همه‌ی
  سؤال‌های یک دوره یکی می‌شدند**. حالا بیت‌ها درست قاطی می‌شوند (`QuestionGenerator.seeded`).
- صدای یک صفحه‌ی تازه را teardownِ صفحه‌ی قبلی می‌کُشد؛ روایت با `view.post(...)` در فریم بعد
  شروع می‌شود.
- لاگ‌های Actions روی استوریج بلاب هستند که از محیط توسعه بسته است. برای دیدن خطا،
  ورک‌فلو باید خودش خطا را جایی بگذارد که از `api.github.com` خوانده شود (مثل ایشو).

## قواعد محتوا و پیمایش

- **هیچ‌چیز قفل نیست.** همه‌ی فصل‌ها و همه‌ی بخش‌ها همیشه بازند؛ «معلم تا کجا درس داده»
  فقط یک نشانک است (ستاره روی چیپ فصل، گره نارنجی روی نقشه)، نه یک قفل.
- **جای کودک نگه داشته می‌شود.** تمرین/کاربرگ/آزمون نیمه‌کاره در `AppState.saveAttempt`
  ذخیره می‌شود (شمارهٔ سؤال + همهٔ جواب‌ها) و دفعهٔ بعد از همان‌جا ادامه می‌یابد؛ گامِ درس هم
  با `saveLessonStep` نگه داشته می‌شود.
- **تولتیپ هوهو پیش‌فرض خاموش است** (`SETTING_TIPS`)؛ متنِ همان جمله بالای صفحه‌ی درس هست.
  والد می‌تواند از پنل والدین روشنش کند، و «پرواز هوهو» را هم همان‌جا خاموش کند.
- **رفت و برگشت آزاد است.** در آزمون و کاربرگ «سؤال قبل/بعد»، و در درس «گام قبل/بعد» و
  کلیک روی نقطه‌های بالای صفحه.
- **متن به گفتار:** نمادهای `× ÷ = ⬜ →` پیش از خواندن در `tts/SpokenText` به واژه‌ی فارسی
  تبدیل می‌شوند، وگرنه موتور گفتار آن‌ها را نمی‌خواند و صدا بریده‌بریده می‌شود.

## کتاب، صفحه به صفحه (روشِ اصلیِ آموزش)

- **بخش جدا برای کتاب نداریم.** هر صفحه‌ی کتاب خودش یک درس است و در همان فهرست بخش‌ها،
  زیر بخشِ خودش، به‌صورت چیپِ «صفحه‌ی ۷» و… دیده می‌شود. کنارش «خلاصه‌ی بخش» هم هست
  (درس‌های بخشی که قبلاً ساخته شده‌اند). اولویت با صفحه‌های کتاب است.
- متن‌ها در `data/Chapter<N>Pages.java` و از `data/PageLessons.java` پیدا می‌شوند
  (`forPage(page)` / `pagesOfSection(chapter, section)`).
- **تصویر PDF در اپ نیست.** هر شکل و جدول و تمرینِ صفحه به‌صورت انیمیشن بازسازی می‌شود
  (`StageSpec.Kind`)، چون کودک باید حس کند با خودِ کتاب کار می‌کند، نه دو چیز جدا.
  متن استخراج‌شده‌ی کتاب برای نوشتن درس‌ها در `project/book/pages.json` نگه داشته شده
  (بیرون از اپ) و `tools/extract_book.py` همان را می‌سازد.
- پیشرفت هر صفحه جدا نگه داشته می‌شود: `AppState.savePageStep` و `markPageDone`.
- انتهای هر صفحه، دکمه‌ی «برویم صفحه‌ی بعد کتاب» هست تا کودک در مسیر کتاب بماند.

### لحن و شکلِ درس (مهم)

- **هوهو معلم است، نه گوینده‌ی کتاب.** هیچ‌وقت «کتاب می‌گوید» نگو؛ بگو «صفحه‌ی هشت،
  تمرین اول…» و بعد مثل معلم توضیح بده: سؤال بپرس، مثال بزن، با کودک بشمار.
- **هر مسئله‌ی صفحه باید بیاید** — با همان شکلِ کتاب، بازسازی‌شده به‌صورت انیمیشن.
- **کودک باید خودش انجام بدهد.** `LessonKind.BUILD` یک جدولِ لمسی می‌دهد که کودک
  خانه‌ها را رنگ می‌کند و شکلِ بعدیِ الگو را می‌سازد؛ هوهو تعدادش را چک می‌کند و بعد
  چیدمانِ خودِ کتاب را رویش نشان می‌دهد (`LessonStep.build(...)`). بالای جدول یک ردیف
  رنگ هست تا کودک دسته‌ها را با رنگ‌های مختلف جدا کند، مثل مداد رنگی روی کتاب.
- **جواب را لو نده.** حالتِ پیش‌فرضِ `LessonStageView` پنهان است؛ روی گام‌های توضیحی
  خودِ درس آن را باز می‌کند. وقتی پنهان است، نوارِ پایینِ صحنه (جایی که جواب نوشته می‌شود)
  کشیده نمی‌شود، آخرین شکلِ الگو و آخرین خانه‌ی جدول جای «؟» می‌گذارند، و
  زیرش می‌نویسد «جوابش را تو پیدا کن».
  فقط بعد از جواب دادن کودک، یا با ورود والدین و زدن «نمایش پاسخ»، باز می‌شود.
- ترتیب هر مسئله: توضیح → «یک مثال دیگر» → نوبتِ کودک (ساختن یا جواب دادن) → بازخورد.
- درس که تمام شد، دکمه‌ی تمرین‌های همان بخش پایینِ کارتِ پایانی هست.

## وضعیت محتوا

- **فصل ۱ صفحه به صفحه آموزش داده می‌شود**: صفحه‌های ۷ تا ۲۴، هر صفحه یک درس با
  انیمیشنِ همان شکل‌های کتاب و پنج جای «خودت بساز». فصل‌های ۲ تا ۸ فصل‌به‌فصل اضافه می‌شوند.
- **هر ۸ فصل خلاصه‌ی بخش هم دارند** — ۴۰ بخش، ۵۳۴ گفتار. هر بخش: چند گام توضیح با انیمیشن،
  «یک مثال دیگر» روی گام‌های توضیحی، دو سؤال تعاملی و یک گام پایانی.
  متن‌ها در `data/Chapter<N>Lessons.java` و همه از `data/Lessons.java` پیدا می‌شوند
  (`Lessons.forSection(chapter, section)` / `Lessons.has(...)`).
- صحنه‌های انیمیشن در `StageSpec.Kind` تعریف می‌شوند و `ui/LessonStageView` آن‌ها را
  می‌کشد: از شمارش دسته‌ای و پله‌های عددی تا ارزش مکانی، محور اعداد، پول، کسر، آرایه‌ی
  ضرب، محیط و مساحت، جمع و تفریق ستونی، نمودار ستونی و ضرب در ده.
- تمرین و کاربرگ و آزمون همه‌ی فصل‌ها کار می‌کند (سؤال‌ها خودکار تولید می‌شوند) و هوهو
  خودش سؤال و راهنما را می‌خواند.
- **سؤال‌های آزمون با کاربرگ یکی نیستند**: هر حالت با یک آفستِ جدا از مولد شروع می‌کند
  (`QuizBuilder.seedOffset`) و آزمون بخش‌ها را هم با ترتیبِ دیگری می‌چیند.
